# Implementation Plan

## High Priority

- [ ] Add greeting utility function
  - Create src/utils/greeting.ts
  - Export function greet(name: string): string
  - Returns 'Hello, {name}!' format
  - Add unit test covering empty string edge case

- [ ] Add farewell utility function
  - Add function farewell(name: string): string
  - Returns 'Goodbye, {name}!' format  
  - Add unit tests

## Completed

(Items will be moved here as completed)
