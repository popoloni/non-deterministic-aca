## Critical Rules

NEVER delete or modify existing tests to make them pass.
If a test fails, fix the implementation, not the test.
The only exception: if a test contains an obvious bug that
contradicts the documented requirements.
