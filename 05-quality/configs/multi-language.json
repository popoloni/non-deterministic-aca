{
  "qualityChecks": [
    "cd frontend && npm run typecheck && npm run lint && npm test",
    "cd backend && python -m pytest && python -m mypy .",
    "docker-compose -f docker-compose.test.yml up --abort-on-container-exit"
  ],
  "maxIterations": 40
}
